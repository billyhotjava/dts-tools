# Copy to `.env` (do not commit secrets).
#
# ODBC DSN (from /etc/odbc.ini)
DM8_DSN=DM8

# Optional: override UID/PWD (if not stored in odbc.ini)
# DM8_UID=SYSDBA
# DM8_PWD=Devops123@

# Connection mode: odbc | jdbc | auto
DM8_MODE=auto

# JDBC fallback (only needed when ODBC is unavailable)
# DM8_JDBC_URL=jdbc:dm://127.0.0.1:5236
# DM8_JDBC_DRIVER=dm.jdbc.driver.DmDriver
# DM8_JDBC_JAR=drivers/jdbc/DmJdbcDriver8.jar
