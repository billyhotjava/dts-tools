# Copy to `.env` (do not commit secrets).
#
# The program auto-loads `.env` from:
# - dm8_excel_etl/.env
# - dm8_excel_etl/config/.env (if present)
#
# Connection mode: odbc | jdbc | auto
DM8_MODE=auto

# ---- ODBC (recommended) ----
# DSN name from /etc/odbc.ini (or user odbc.ini)
DM8_DSN=DM8
# If UID/PWD are not stored in odbc.ini, set them here:
DM8_UID=
DM8_PWD=

# ---- JDBC (when DM8_MODE=jdbc, or fallback when DM8_MODE=auto) ----
# Note: inside Docker, `127.0.0.1` refers to the container itself.
# If DM8 runs on the host, either:
# - use `network_mode: host` in docker-compose, OR
# - set DM8_JDBC_URL to the host IP (e.g. jdbc:dm://192.168.x.x:5236)
DM8_JDBC_URL=jdbc:dm://127.0.0.1:5236
DM8_JDBC_DRIVER=dm.jdbc.driver.DmDriver
# Jar path can be absolute, or relative to dm8_excel_etl/ (recommended with volume mount)
DM8_JDBC_JAR=drivers/jdbc/DmJdbcDriver8.jar
